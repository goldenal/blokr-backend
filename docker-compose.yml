services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: blokr
      POSTGRES_PASSWORD: blokr
      POSTGRES_DB: blokr
    ports:
      - '5432:5432'
    volumes:
      - blokr_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U blokr -d blokr']
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  blokr_pgdata:
